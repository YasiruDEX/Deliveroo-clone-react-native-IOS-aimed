import { lazy } from 'react';
import { defineLocaleResourceBundle, definePlugin } from 'sanity';
import { route } from 'sanity/router';
import { EyeOpenIcon } from '@sanity/icons';
const visionLocaleNamespace = "vision";
const visionUsEnglishLocaleBundle = defineLocaleResourceBundle({
  locale: "en-US",
  namespace: visionLocaleNamespace,
  resources: () => import('./resources-bPLLJ09x.js')
});
const visionTool = definePlugin(options => {
  const {
    name,
    title,
    icon,
    ...config
  } = options || {};
  return {
    name: "@sanity/vision",
    tools: [{
      name: name || "vision",
      title: title || "Vision",
      icon: icon || EyeOpenIcon,
      component: lazy(() => import('./SanityVision-b2S0ttwz.js')),
      options: config,
      router: route.create("/*")
    }],
    i18n: {
      bundles: [visionUsEnglishLocaleBundle]
    }
  };
});
export { visionLocaleNamespace, visionTool };
//# sourceMappingURL=index-WUX2qElD.js.map
