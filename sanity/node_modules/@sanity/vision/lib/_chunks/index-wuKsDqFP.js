'use strict';

var React = require('react');
var sanity = require('sanity');
var router = require('sanity/router');
var icons = require('@sanity/icons');
const visionLocaleNamespace = "vision";
const visionUsEnglishLocaleBundle = sanity.defineLocaleResourceBundle({
  locale: "en-US",
  namespace: visionLocaleNamespace,
  resources: () => Promise.resolve().then(function () {
    return require('./resources-l8tqSYTA.js');
  })
});
const visionTool = sanity.definePlugin(options => {
  const {
    name,
    title,
    icon,
    ...config
  } = options || {};
  return {
    name: "@sanity/vision",
    tools: [{
      name: name || "vision",
      title: title || "Vision",
      icon: icon || icons.EyeOpenIcon,
      component: React.lazy(() => Promise.resolve().then(function () {
        return require('./SanityVision-CzPwJrct.js');
      })),
      options: config,
      router: router.route.create("/*")
    }],
    i18n: {
      bundles: [visionUsEnglishLocaleBundle]
    }
  };
});
exports.visionLocaleNamespace = visionLocaleNamespace;
exports.visionTool = visionTool;
//# sourceMappingURL=index-wuKsDqFP.js.map
